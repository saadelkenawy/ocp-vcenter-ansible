[defaults]
inventory           = inventory.ini
host_key_checking   = False
retry_files_enabled = False
stdout_callback     = debug
collections_path    = ~/.ansible/collections
vault_password_file = /home/saad/ocp-vcenter-ansible/.vault_pass
# Rollback for stdout (ansible package was not icluded that makes output more readable)

[vault]
# Store your vault password in a file for convenience (optional):
# vault_password_file = .vault_pass
# Then run without --ask-vault-pass

[ssh_connection]
pipelining = True
